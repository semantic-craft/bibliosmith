# 12 构建与校验

构建前运行 `npm run preflight:template`、`npm run lint:publication`、`npm run lint:assets`、`npm run cover:check`、`npm run reader:check`。清理或重建旧 staging，避免旧 XHTML 或旧资源污染门禁结果。构建后运行 `npm run check:epub`。
