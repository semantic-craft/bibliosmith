# EPUB 制作质量经验 / EPUB Production Quality Lessons

## 来源 / Source

本文件沉淀自《黑人北极探险家》《幽灵海盗》等 EPUB 制作过程中的问题、返工和用户反馈，作为后续所有 EPUB 项目的通用质量规则。

## 字体 / Fonts

- 默认不要在 EPUB CSS 中写死 `font-family`。
- 如果阅读器尊重出版商字体，写死字体会导致用户无法在 App 中切换字体。
- 不能因为某个字体好看就直接嵌入完整中文字体；完整 CJK 字体通常几十 MB，会显著膨胀 EPUB。
- 若必须指定字体，使用字体子集化，并记录授权、体积和 fallback。
- `霞鹜文楷/LXGW WenKai` 可作为审美方向，但不应完整塞入普通 EPUB。

## 封面 / Cover

- EPUB 必须有封面页和 OPF `cover-image`。
- 封面图片应压缩到合理体积。一本 280KB 正文的书，不应使用 3MB 未压缩封面。
- 推荐 EPUB 内封面使用 JPG，保留高清 PNG/PSD 等作为工程素材，不直接打包。
- 封面文字需要人工或确定性排版核验，图像模型不可靠地处理精确居中。

## Metadata 与版本说明 / Metadata and Book Info

- 读者看到的书籍详情首先应是本项目版本信息，不只是原书信息。
- 公版或授权项目必须包含：`BiblioSmith 书坊 + 个人名`、译制时间、公版来源 URL、公版说明、作者信息、原书信息、本书简介。`publication_mode=private_use` 项目必须改用 `modes/private_use` 覆盖层：`参考public-domain-books-translation 开源项目 个人自制`、个人自用/不传播/不商业使用、风险边界、本地书源证据，且不得含公版说明。
- 品牌名必须全书一致，不得出现旧名称残留，例如 `BiblioSmith 翻译组`。

## 标题与排版 / Headings and Layout

- 手机窄屏下章节标题不得过大。
- `第X章` 和章节说明的字号要一致或视觉协调，不能一个小一个大。
- 正文行距、缩进、段落间距要适合中文长文阅读。
- 样章阶段必须检查标题、正文、封面、版本说明在阅读器中的实际效果。
- 读者可见章节标题必须有源文依据。如果原书某章只有编号、罗马数字或简单题名，不得为 EPUB 自创解释性小标题；章节概括应进入 `title_note`、制作说明或 QA 记录。
- 原书已有的附录题名、小标题、题辞或署名可以保留，但必须能追溯到源文结构，不能把 AI 概括当作原书副标题。

## 精修复查 / Refinement Review

- EPUBCheck 通过不等于书稿完成。最终交付前仍需检查可见标题、metadata、出版文本、源文尾部工件、AI 输出残留和目标语言排版。
- 原始来源证据和出版文本是不同范围。`source/source_text_raw.txt` 应尽量保留下载原貌；BOM、乱码、异常空格和不当标点应在 `frontmatter/`、`chapters/final/`、`metadata/` 和 EPUB XHTML 等读者可见范围内清零。
- 可复用的精修扫描脚本应输出到 `qa/refinement/refinement_check.json`，并把任何允许保留的例外写入人工 QA 记录。
- 术语呈现事故要回填为机器可查规则。若某本书出现“正文音译历史术语”“正文堆 `中文译名（source term）`”“把制度身份泛译成现代政治词”等问题，不能只在该词上修补；必须更新术语表字段、章节译后控制和终稿门禁，让后续章节按禁用写法扫描。
- 模板示例不得随手使用未核定的音译。示例会被后续 agent 当作推荐译法；高风险术语示例应优先展示“中文正文 + 注号 + 术语说明”的形态，并明确禁用或不推荐的正文写法。

## 评审 / Review

- 主执行 AI 容易忽略自己刚做过的错误，必须有独立评审。
- 双 Agent 评审必须分工：一个看翻译内容，一个看 EPUB 工程和排版。
- 评审发现 P0/P1/P2 问题时，必须回退到对应阶段，不得只解释。
