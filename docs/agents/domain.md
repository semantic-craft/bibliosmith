# Domain Docs

This repo uses a single-context layout.

Before architecture, bug diagnosis, or TDD work, read root `CONTEXT.md` if it exists and relevant ADRs under `docs/adr/`.

If these files do not exist, proceed silently. The domain-modeling flow can create them later when real terms or decisions need to be captured.
