# Folder-scoped book OCR skill

This repository now carries a root `SKILL.md` plus `AGENTS.md` so future agents immediately understand that the folder is a book OCR and document-conversion pipeline, not a translation project or general LLM application. The skill is deliberately folder-scoped: it records local workflow, safety rules, and terminology without installing anything globally or changing shared skill surfaces.
